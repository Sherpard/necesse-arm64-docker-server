###############################################
# Necesse Dedicated Server Configuration
# Sensible defaults for publishing as a template
###############################################

########## Networking ##########

# The UDP port your server listens on.
# Open this in your firewall/router if needed.
SERVER_PORT=14159

########## Server Access ##########

# The total number of player slots available on the server.
SLOTS=8

# The name of the player who will automatically have owner permissions.
# Change this to your preferred admin username.
OWNER=admin

# Optional password to protect the server. Leave blank for no password.
PASSWORD=

# The server message shown to players on connect.
# You can include line breaks using \\n.
MOTD=Necesse Server\\nWelcome to your world. Customize this message in .env.


########## Behavior ##########

# Pause the game when there are no players connected.
# 1 = Pause (recommended for public servers), 0 = Keep running
PAUSE_WHEN_EMPTY=1

# Give clients full control (disables anti-cheat checks).
# 1 = Offload processing to clients (faster, less secure)
# 0 = Server validates actions (safer — recommended)
GIVE_CLIENTS_POWER=0

# --- Logging & Monitoring ---

# Log handling strategy:
# "docker" = write logs to stdout only (recommended)
# "files"  = write log files to /home/steam/data/logs as well
LOG_MODE=docker

# Compress save files to reduce disk usage.
# 1 = Enabled (recommended), 0 = Disabled
ZIP_SAVES=1

# Server console language (only affects log output).
LANGUAGE=en


########## Data Paths ##########

# Optional settings file for the server.
# This should exist in the data directory.
# SETTINGS_FILE=server-settings.cfg

# The world name to load automatically at startup.
# If missing, the server will prompt for world selection (headless mode will fail).
WORLD_NAME=MyWorld

# Use local directory for all data (saves, logs, cache) instead of specifying -datadir.
# 1 = Use -localdir (within /home/steam/server)
# 0 = Use -datadir (points to /home/steam/data) — recommended
LOCAL_DIR=0


########## Update Settings ##########

# Automatically fetch and install the latest Necesse server version
# before starting up. Disable if you prefer manual control.
AUTO_UPDATE=false


########## JVM / Advanced ##########

# Maximum memory allocation for the Java process.
# Adjust based on your hardware (examples: 512M, 1G, 2G).
JAVA_MEMORY=2G


###############################################
# Volumes (set in docker-compose.yml)
# ./data  →  /home/steam/data   (saves, configs, logs)
# ./cache →  /home/steam/cache  (download cache)
###############################################

